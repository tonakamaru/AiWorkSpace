# Luna 固定で ChatGPT に送信する

## 実行

PowerShell で次のように実行します。指定するのは送信先の名前と文章だけです。

```powershell
cd G:\creation\AiWorkSpace\chatgpt-cli
.\Send-ChatGPT.ps1 -To github -Message 'CLIからのテストです。受信しましたとだけ返してください。'
```

別のフォルダからでも使えます。

```powershell
& 'G:\creation\AiWorkSpace\chatgpt-cli\Send-ChatGPT.ps1' -To github -Message 'こんにちは'
```

複数行は PowerShell のヒアストリングで渡せます。

```powershell
$message = @'
AiWorkSpaceの状態を確認してください。
今回は読み取りだけでお願いします。
'@
.\Send-ChatGPT.ps1 -To github -Message $message
```

通常の文章はシングルクォートで囲むと、`$` などが PowerShell で展開されません。文中のシングルクォートは2つ重ねるか、上記のヒアストリングを使ってください。

## 送信先 JSON

`chatgpt-targets.json` に送信先を登録します。

```json
{
  "github": {
    "title": "GitHubアクセス確認",
    "threadId": "6aae4a91-41f8-83e8-be3f-43d231427920"
  }
}
```

`github` が `-To` に渡す名前です。現在は `github`、`consult`、`Astra`を登録しています。会話名を変更した場合は JSON の title も更新してください。UTF-8 で保存します。

会話IDは ChatGPT の会話URLの `/c/` より後の部分です。対象のIDとタイトルは会話一覧などで確認して登録してください。入力した名前が存在しない場合、CLI を起動する前に停止します。

## 固定している設定

- 操作担当モデル：`gpt-5.6-luna`
- 推論量：`low`
- 作業フォルダ：このスクリプトがあるフォルダ
- ローカルのサンドボックス：read-only（外部の送信操作を禁止する設定ではありません）
- 送信先は既存会話のみ。送信先の回答モデルは変更しません。
- 文章は JSON にして標準入力で CLI に渡すため、本文をシェルコードとして実行しません。
- 送信は1回。送信担当が終了したら、スクリプトが最大5分、15秒以上の間隔で読み取りだけを続けます。履歴取得・送信の各 CLI 処理は最大5分で打ち切ります。

## 必須条件と確認状況

Codex デスクトップを起動しておいてください。ブラウザや送信先のタブを開く必要はありません。CLI の実行時にデスクトップ同梱の公式 `codex_app` MCP サーバーを有効にし、履歴取得と返答確認には読み取り、送信には送信の1ツールだけを公開します。永続的な CLI 設定は変更しません。

アプリ内ターミナルでは接続先を環境変数から取得します。通常の PowerShell では `.chatgpt-bridge.json` に保存した接続先を使います。この PC の現在のアプリ接続先は設定済みです。このファイルは PC・アプリ起動状態に依存するため Git の対象から除外しています。

アプリ再起動後に接続エラーが出る場合は、Codex アプリ内ターミナルで次を実行するか、デスクトップのタスクに同じスクリプトの実行を依頼してください。

```powershell
& 'G:\creation\AiWorkSpace\chatgpt-cli\Initialize-ChatGPTBridge.ps1'
```

アプリを終了した状態では送信できません。接続可否をモデル起動前に確認し、接続できなければ停止します。アプリ更新で同梱 MCP の仕様が変わった場合は修正が必要になる可能性があります。

2026年9月19日、別の Luna CLI セッションから Astra 宛ての実送信に成功し、送信先の新しい返答も確認済みです。

## 返答と履歴

呼び出し方は同じです。標準出力には、今回のメッセージに対応する完成済みの返答本文だけを出します。トークン単位のストリーミングではありません。進捗・診断・COMPLETED は標準エラーに出します。

```powershell
$reply = .\Send-ChatGPT.ps1 -To Astra -Message 'こんにちは'
$reply
```

PowerShell でファイルに残す場合:

```powershell
.\Send-ChatGPT.ps1 -To Astra -Message 'こんにちは' 2> progress.log |
    Set-Content -Encoding UTF8 reply.txt
```

PowerShell 5.1 の `>` は既定で UTF-16 になるため、返答ファイルを UTF-8 にしたい場合は `Set-Content -Encoding UTF8` を使います。履歴 JSONL は PowerShell のバージョンに関係なく UTF-8（BOMなし）です。

1. 対象 ID の会話を全ページ取得します。タイトル・種類・待機状態を確認します。
2. `history/<threadId>.jsonl` に取得履歴を古い順で保存し、ディスクへの書き込み完了後に送信します。
3. 1回送信し、新しいターンのユーザー本文が送信内容と完全一致することと、返答の完了を確認します。
4. 取得した新しいターンを JSONL に追記し、完成した返答を標準出力へ出します。

履歴取得・保存に失敗した場合は送信しません。既存の JSONL は上書きせず、同じターン・同じ内容は重複保存しません。回答中→完了などの変化は同じターン ID の更新行として末尾へ追記します。現在の履歴として読む場合は `turn.id` ごとに最後の行を採用します。

1行の構造（本文中の改行は JSON の `\n` として保存）:

```json
{"schemaVersion":1,"type":"turn","threadId":"会話ID","title":"会話名","observedAt":"取得日時ISO8601","turn":{"id":"ターンID","status":"completed","items":[{"type":"userMessage","id":"入力ID","content":[{"type":"text","text":"質問"}]},{"type":"agentMessage","id":"返答ID","text":"返答"}]}}
```

`turn` は取得ツールの実データです。Luna に履歴の要約や JSON の再生成をさせません。`read_thread` が公開する会話の範囲を保存するため、ChatGPT の完全なデータエクスポートではありません。添付ファイル本体などは保存しません。本文の取得上限は各項目2万文字です。それを超える本文やサービス側で省略された内容は完全には保存できません。1ページ10ターンずつ取得します。

返答を確認できない場合は標準出力に成功結果を出さず、エラーで終了します。`SUBMITTED_UNCONFIRMED` や送信段階のエラーは、実際には送信されている可能性があります。自動再送はしません。送信先を確認してから判断してください。取得できた途中のターンは保存され、遅れて届いた返答は次回実行の送信前の履歴取得でも回収できます。

同じ履歴ディレクトリを使う別セッションの同時実行は、会話 ID ごとの `.lock` で止めます。強制終了でロックが残った場合は、その送信プロセスが終了したことを確認してから該当する `.lock` だけを削除してください。別のフォルダへコピーしたスクリプトや手動のブラウザ操作とは排他されません。

`history/` は Git 管理対象外です。利用枠は履歴取得と送信、および必要に応じた返答確認の Luna 実行で消費します。長い会話は取得に時間がかかります。

## ローカル検証

```powershell
.\Test-SendChatGPT.ps1
```

実送信せず、複数ページの保存順、送信前の保存、差分追記、古い返答の除外、取得漏れ・壊れたログ・同時実行の拒否、返答の完全一致を検証します。モデル部分は模擬応答です。実際の CLI・MCP 接続と PowerShell の文字列受け渡しは別途、実送信テストで確認します。

内部では [Codex CLI の JSONL イベント出力](https://learn.chatgpt.com/docs/non-interactive-mode) から MCP の取得結果を読みます。
2026年9月19日、Windows PowerShell 5.1 の通常コマンド1回で、送信前の履歴保存・実送信・遅延した返答の取得・標準出力への本文のみの出力・JSONL への重複なし追記・正常終了まで確認済みです。日本語・改行・引用符・シェル風文字列の保持も確認しました。
